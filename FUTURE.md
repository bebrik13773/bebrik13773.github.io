# Next Steps

- Подкрутить экономику перевода очков из `fly-beaver`, если понадобится коэффициент вместо текущего прямого переноса `1:1`.
- Добавить отдельные быстрые действия в админке для ручного снятия банов и просмотра истории забегов без SQL.
- При желании вынести общие API-хелперы из `index.html` и `fly-beaver.html` в отдельный клиентский модуль.
- Если нагрузка вырастет, перевести часть статистики админки на кэшированные агрегаты вместо живых `COUNT(*)`.
